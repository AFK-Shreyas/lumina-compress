/** Primary phrases for meta keywords and on-page copy (use naturally). */
export const SEO_KEYWORDS = [
  'image compressor',
  'compress image online',
  'reduce image size',
  'jpg compressor',
  'png compressor',
  'photo compressor',
] as const

export const SEO_KEYWORDS_STRING = [...SEO_KEYWORDS].join(', ')

export const SITE_NAME = 'Lumina Compress'

/** Replace with your real inbox before production (AdSense / policy pages). */
export const SUPPORT_EMAIL = 'support@luminacompress.com'

export const DEFAULT_OG_TYPE = 'website' as const
